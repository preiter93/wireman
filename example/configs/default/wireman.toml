includes = [
    "$HOME/Rust/wireman/example",
]
files = [
    "grpc_simple/debugger.proto",
    "grpc_simple/greeter.proto",
    "grpc_simple/productfinder.proto",
    "grpc_simple/timekeeper.proto",
]

[history]
directory = "$HOME/Rust/wireman/example/history"

[server]
default_address = "http://localhost:50051"
