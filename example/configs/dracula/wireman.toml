includes = [
    "$HOME/Rust/wireman/example",
]
files = [
    "server/simple/debugger.proto",
    "server/simple/greeter.proto",
    "server/simple/productfinder.proto",
    "server/simple/timekeeper.proto",
]

[server]
default_address = "http://localhost:50051"

[history]
directory = "$HOME/Rust/wireman/example/history"

[ui]
skin = "$PWD/../wireman-theme/resources/skins/dracula.toml"
